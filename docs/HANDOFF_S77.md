# HANDOFF S77 -> S78
DATE: 2026-07-18 | HEAD: `65002a8` + close commits (verify ls-remote) | MODEL: Fable 5
Read ONCE. Standing rules: docs/GNI_RULES.md by ID (current through R-S76-3 IN BOOK; R-S77-1/2/3 pending below).
Contract: docs/CONTRACT.md (unchanged). 7 commits, zero reverts.

## 1. STATE (<=10 lines)
STRATEGY RULED (James): Lens/L-CLIFF WAITS until GNI subpage integrity is finished error-free.
  Order = SUBPAGE-TRUTH -> F22 -> WEIGHT-PRIOR/GATE-DESIGN -> others.
SUBPAGE-TRUTH BUILD PHASE ~DONE (C1-C6): 7-LAYER SWOT ruled A (honest-rewrite all, build nothing)
  -- L6 lang-normalization was a GHOST (dropped), L2 = pattern category, L3/L5 relocated truthfully,
  SHA-256 chain proven LIVE (main.py:24 import). D-2 CLOSED. Stale-count + F-EMA-COPY diseases
  EXTINCT tree-wide by grep. /predictions GPVS-free. avgQ honest.
TRANS-COUNT-CERT CORRECTED: Jul 17 crons ran pre-S75-push sha a7833854 (same resolution S76 found).
  Log 273 vs DB 360 = old bug as documented, another honest fossil. TRUE post-patch run still pending.
RULES-APPEND closed `4e33941`. Remaining SUBPAGE-TRUTH: F3, WORD-CONV, PHASE-NARR, RE-CERT.

## 2. DELTA (<=15 lines)
| Item | What | Proof |
|------|------|-------|
| `4e33941` | R-S76-1/2/3 appended | grep 3 + tail |
| C1 `e26b893` | devops: L2 real stack, 81 count, L4 real blended-EMA | grep 70-pattern/x1.1 = 0 |
| C2 `81bf48c` | patterns:56 EMA copy -> real 0.7/0.3 blend | grep 1.1x = 0 |
| C3 `d99405a` | feedback SWOT count-free (numbers removed, not chased) | grep 70-pattern = 0 |
| C4 `9ae5f9c` | D-2 CLOSED: methodology 7-layer -> real stack; about + dev-hub 70->81 | tree grep = 0 |
| C5 `5d79c45` | predictions: info box = debate-verifier mechanics (-2%/-1% thresholds), footer relabeled | grep GPVS = 0 |
| C6 `65002a8` | patterns Avg Quality labeled (Last 7 Runs) | grep Last 7 Runs = 1 |
| SWOT | 7-layer verdict: 3 live / 3 mislocated / 1 ghost; ruling A ratified ("your call") | this session |
| L7 proof | audit_trail imported by main.py:24 + self_healing + self_bias_gate | grep |
| Horizons | long_verify_days default 180 confirmed (mad_runner:206) | grep |
| CRLF live hit | C5 anchor-2 \n failed count=0; EOL-derived retry PATCHED; chained build/commit ran once on unpatched tree (harmless) | transcript |

## 3. QUEUE (<=25 lines)
| ID | Task | First move | Gate | Trust |
|----|------|-----------|------|-------|
| RULES-APPEND | R-S77-1/2/3 below + commit (landing gate) | tail docs/GNI_RULES.md | James | - |
| RE-CERT | screenshot pass over 8 touched pages post-deploy (devops, patterns, feedback, about, dev-hub, methodology, predictions) D-stamp | browser after Vercel deploy | James | - |
| F3 | legend flag -- RE-READ census spec (S69/S70 records) before touching; 3 candidate files: correlations/map/source-health | conversation_search census F3 | James | B |
| WORD-CONV | site-wide "prediction" word convention ruling | James rules terminology | James | V(S76) |
| PHASE-NARR | Phase-narrative provenance ruling | read + ruling | James | B |
| WEIGHT-PRIOR | 1.00 seeds outrank earned; EMA owns weight -- read seed_roster_weights + ruling | read source_weights seeding | James | V(eyes) |
| TRANS-COUNT-CERT | next Intelligence cron: headSha MUST != a7833854, then grep 'Deduplication' vs DB | gh run list wf "GNI Intelligence Pipeline" -L1 | - | V(bytes) |
| CRED-TOTAL-WATCH | writer B fire (stochastic ~1-in-10): CI print then SQL GROUP BY gpvs_total = spread | gh log grep | - | V(bytes) |
| GATE-DESIGN | stochastic %10 gate SWOT; D-11 adjacent | SWOT w/ D-11 | James | V(bytes) |
| F20-CERT | close vs 43%/48% baseline | /correlations growth | - | V(cert) |
| GT5-T-WATCH | 1wk digest before T=2 (~Jul 24) | digest read | James | - |
| FED-DOE-WATCH | lineage decay; grep next 2-3 MAD verdicts | gh run view grep | - | V(S74) |
| FT-GAP-B-CERT | next real AUTO-ACTIVATED line | trace read | - | V(tests) |
| F22 | /debate withheld-coaching badge (after SUBPAGE-TRUTH per ruling) | grep grounding persistence first | James | V(S73) |
| D-11 | escalation recalibration SWOT (+ GATE-DESIGN) | POST-CLIFF | James | V(S74) |
| DEAD-COLS | unread selects | leads | - | B |
| J-RULINGS | J-4 probe, J-7 scorer (Aug 9); J-1 sunsets post-cliff | - | James | - |
| L-CLIFF/CLIFF-CODE Aug 16 | WAITS per S77 ruling until subpage integrity done; OC-A ~Jul 24 / CERT ~Aug 2 / keyfile Aug 9 | - | James | - |
| K-WATCH-NS / SAN-DEAD / CENSUS-2 / K-CAND / YAKE-KM / DEAD-PILLAR / L4-COUNT / F-CASE / F-KEY / SOLV-6 / SRC-EXPAND / U-W / I-WATCH / A-VLOG / SRC-PHI / GT-6(banked) | unchanged | - | - | - |

## 4. UNKNOWNS (<=8 lines)
| Fact | Trust | Resolve by |
|------|-------|-----------|
| TRANS-COUNT live behavior | patched, no post-patch run EXISTS yet | TRANS-COUNT-CERT |
| Writer B live output | code read only, never fired | CRED-TOTAL-WATCH |
| WEIGHT-PRIOR bug or design | screenshot only | WEIGHT-PRIOR ruling |
| F3 spec content | not in S77 context, 3 candidate files | F3 census re-read |
| debate verifier backlog claim | copy claim, unread | next verifier read |
| Fed/DoE lineage decay / T=3 evidence | designed, unobserved / one sample | watches |

## 5. TRAPS (<=8 lines)
- a7833854 = pre-S75-push main; EVERY Jul 17 run is pre-patch. Check headSha BEFORE reading any
  run's funnel prints (S77 read one for nothing -- ancestry check comes FIRST).
- CRLF: multi-line anchors derive nl (R-S76-1) -- hit live again in C5. Failed assert = zero bytes,
  but chained build/commit after it runs on the UNPATCHED tree (R-S77-1). Gate the chain on PATCHED.
- /predictions '52 correct' tile is LIVE-COMPUTED (L41 filter) -- attribution was the bug, now fixed;
  never re-flag the number as fossil. /about '81' is hydrated-with-fallback -- correct, not stale.
- Uniform-102 gpvs_total rows still honest fossils until writer B fires. 466 vs 111 both true.
- devops pipelines[0] said 81 all along -- page self-contradiction is gone, don't hunt it.

## 6. LOAD CHECK - next AI echoes EXACTLY these 5 lines, nothing more
HEAD = `65002a8`+close commits TREE CLEAN -- SUBPAGE-TRUTH C1-C6 shipped (D-2 closed, diseases extinct), SWOT ruled A, Lens WAITS for integrity per James
TOP3 = RULES-APPEND (R-S77-1/2/3), RE-CERT screenshots (8 pages post-deploy), F3 census spec re-read
DEADLINE = GT5-T-WATCH ~Jul 24 / OC-A ~Jul 24 / CERT ~Aug 2 / keyfile Aug 9 / Groq cliff Aug 16 (L-CLIFF gated on integrity completion)
TRAP = a7833854 = pre-patch (ancestry check FIRST); CRLF anchors derive nl; gate chained commit on PATCHED print; 52-correct tile is live math
FIRST MOVE = ls-remote + git status; then gh run list wf "GNI Intelligence Pipeline" -L1 + headSha ancestry check

---
RULES APPEND for docs/GNI_RULES.md (at S78 open, landing gate):
R-S77-1: Chained build/verify/commit commands after a patch script run UNCONDITIONALLY --
  a failed assert writes zero bytes but the chain proceeds on the unpatched tree. Gate the
  chain on the PATCHED print, or run patch and commit as separate pastes. (C5 first attempt.)
R-S77-2: For count claims in prose copy, removing the number beats hydrating it -- prose that
  names no count can never rot, and plumbing-to-prose is bad engineering. Hydrate only where
  a number is displayed as a stat. (Feedback SWOT: two mentions cured count-free.)
R-S77-3: A live-computed stat can still lie by ATTRIBUTION -- verify the label's system name
  against the data source, not just the number's math. ('52 correct' was live math under a
  GPVS label while reading debate_predictions.)

DIARY S77 (<=10 lines):
The session that asked where we were going before taking another step. A strategy review found
two healthy arcs and one sliding deadline; James ruled: truth first, Lens waits. Then the SWOT
put seven claimed layers under the bytes -- three live, three misplaced, one ghost -- and the
ruling was the rare kind where honesty made the page stronger, because the real stack outclassed
the paper one. Six copy commits later, two diseases are extinct by grep: no stale count survives,
no imaginary formula remains. The wrong newline bit us once more and this time taught us that
the commands AFTER a dead assert are the real hazard. A prediction tile turned out to be honest
math wearing the wrong name-tag. Seven commits, one strategy, one ghost buried, zero reverts.
The site now describes the system it actually is. Fist bump.
