# HANDOFF S70 -> S71
DATE: 2026-07-16 | HEAD: `8b03ed3` (verify ls-remote) | MODEL: Fable 5
Read ONCE. Standing rules: GNI_RULES.md by ID (current through R-S69-3; R-S70-1/2 pending append below).
Contract: docs/CONTRACT.md (unchanged). !! TREE CLEAN. 11 commits this session, all docs/copy — no shifter shipped.

## 1. STATE (<=10 lines)
FT-GAP-A CLEAN RUN #2 (Jul 16 morning): Irrawaddy fetch-OK/0 -> AllAfrica 20 (2nd firing),
  AP error -> Defense News 6, 5 sources [STALE-GATED] raw>0/yield=0 correctly healthy-not-down.
  ONE more clean run (evening) opens the window. NEW ORDER (James-delegated, ratify at ship):
  FT-GAP-A close -> SRC-INTEGRITY -> FT-GAP-B -> DET-DEAD.
RECENCY #3 healthy (CAPTURE-LAG drops 19.6h-1974h; 360->250->247->22; no regression).
SRC arc COMPLETE on disk: register 42/42 pass threshold, Fox World KEEP (60, ritual satisfied),
  integrity design I-1..I-9 evidence-backed (writer B silent-dead since Jul 8; fossil graveyard
  TOPS /health credibility board). MAD solver: DRIFT status live, print-only (D-7 exact scope).
SUBPAGE-IC: certification ledger born (docs/SUBPAGE_CERTIFICATION.md v2); batch 1 done;
  F1 RULED 81-funnel -> TRUTH unlocked; deadline = before ~Aug 2 model window.
FABRICATION: today's debate = GT-5 exhibit A (Fed/DoE denial fabricated -> public action rec).

## 2. DELTA (<=15 lines)
| Item | What | Proof |
|------|------|-------|
| `c3ded2d` | RULES-FIX: R-S63 triple-block dedupe (D-9 dead) + R-S69-1..3 appended | counts 1/1/1 |
| `8733341` | SUBPAGE-STALE: F2 21->28(bytes), F6 33->35, F7 15->37, F9 verifier-live, F14 honest-NEUTRAL; F4 found paid | browser-cert /comparison /validation-log |
| `640ea12` | COSMETIC: F10 $0 restored x7 (sed-ate-dollar) + comparison AGREE tail sibling | browser |
| `95f0f63`+`6adc60a` | SRC docs: integrity design I-1..7 + register v2 + evidence I-8/I-9 | Supabase reads |
| `f596390` | Debt: D-7 corrected OPEN (solver wired via mad_protocol:43, FLOOR_HIT print-only), F-TILES dissolved into I-9 (Stimson=CF-403 dead, fossil DB row), D-10 CLOSED (staging check 9/9 in log) | bytes+log |
| `49bfadb`+`8b03ed3` | CERT ledger: stub accident then FULL v2 restore (R-S70-2 born from it) | wc 68 |
| Rulings (delegated, James ratifies) | F1=81-funnel; F15=FAMILY relabel (/comparison /researcher /research /scenarios); R1 quota->floors (GEO-primary, FIN>=5 TECH>=6); R2 NYT TIER1; R3 Crisis/Amnesty/ICIJ TIER1 (drift); R4 WATCH pair keep; F13 lean re-source+archive | S70 records |
| Fox World | Re-admission lineage CLOSED: followed Lens usage (James memory) + scored 60 in bytes = ritual satisfied, KEEP | register |
| Fabrication audit | Debate vs 354-trace: Fed/DoE denial, Brazil 25% tariff, Treasury-SPR (wrong in reality), quant chains ALL fabricated; Swan's 2 citations VERIFIED REAL (REF-3BE4, REF-B029, Stage-1 rejects by design); GROUNDING SHADOW 22 consultant+6 arb, shadow no-action | bytes |
| CERT batch 1 | /predictions D (475/193/282 coherent), /scenarios D; F16 researcher empty chart, F17 research CI-vs-RUNS, F18 correlations 290-vs-282, F19 predictions 52-correct-vs-61% | screenshots |
| Tab scope | James census: 12 tabbed pages; D stamp = default + EVERY tab (in ledger) | ledger |
| Quota | 88% worst-account (morning 87948) recurring — storm cadence, watch only | Telegram |

## 3. QUEUE (<=25 lines)
| ID | Task | First move | Gate | Trust |
|----|------|-----------|------|-------|
| RULES-APPEND | R-S70-1/2 into GNI_RULES.md + commit w/ this handoff | tail check first (R-S70-2!) | James | - |
| TRACE-READ-EVE | Jul 16 evening run = FINAL A-clean confirmation -> window opens | Actions log when landed | - | - |
| F15-FAMILY | relabel "Last 10 Runs" on 4 pages — top easy win, copy-only | locate greps (4 strings) | James | V(S70) |
| SRC-INTEGRITY | FIRST in window: single-writer + I-3 wins/total + case-norm + 42-seed + __main__ kill + I-9 graveyard purge SQL + /health roster filter + TIER-TRUE (R2/R3) + I-8 root-cause | re-read both files at window-open (BEV) | James ratify order+R1-R4 | V(design+evidence) |
| SUBPAGE-TRUTH | OPENS WITH 7-LAYER SWOT vs philosophies (James mandate); then hydration extension (F1 ruled) + D-2 rewrite + F3 legend; not-true-shifter (stats path) | SWOT session | James | V(bytes) |
| CERT-BATCHES | ~5 sittings, 25 routes; D=default+tabs; each batch earns H+W per family | batch 2: James picks 5 | James | - |
| F16-F19 | byte-traces: researcher chart, research CI, correlations 290-pop, predictions metric | 1 grep each | James picks | B |
| F13/F12 | /alerts build: counter->adaptive-log source, CRITICAL archive-unless-cheap, F12 math | after ratify | James | V(S69 trace) |
| GT-5 | digests #4-5 passive THEN enforce ruling w/ exhibit A (Fed/DoE public action-rec) + S69 research inputs | evening digest | James | - |
| J-RULINGS | STILL OPEN 3rd session: J-1 manual-MAD caution (overdue Jun 12) lift/keep; J-6 O4 SQL done? | James memory | James | - |
| OC-A | ~Jul 24 (8d) | - | James | - |
| U-AUG9 | keyfile marathon Aug 9: GROQ_MAD_MODEL + U1 pick (~Aug 2 watch) + J-4 probe + J-7 scorer | - | James | - |
| CLIFF-CODE / L-CLIFF | Aug 16 = 31d; Lens opener SOON, D-8 artifact verify FIRST MOVE there | Lens session | James | - |
| GT-3 / DET-DEAD | after window chain; DET-DEAD merge design owns 3-array family | - | James | B |
| K-WATCH-NS / SAN-DEAD / CENSUS-2 / K-CAND / YAKE-KM / DEAD-PILLAR / L4-COUNT / F-CASE / F-KEY / SOLV-6 / SRC-EXPAND(FIN/TECH only per R1) / U-W / I-WATCH / A-VLOG | unchanged | - | - | - |

## 4. UNKNOWNS (<=8 lines)
| Fact | Trust | Resolve by |
|------|-------|-----------|
| Writer B failure root cause (silent since Jul 8; main.py:252 calls it) | evidenced | SRC-INTEGRITY ship |
| F18 290-vs-282 population source | flagged | 1 grep correlations API |
| F19 52-correct(18.4%) vs 61% 7d metric | flagged | byte-trace both readers |
| C2 yield>0 gate | live, unexercised | passive |
| Evening run result | pending | TRACE-READ-EVE |
| James ratifications: R1-R4, F1, F13, F15, window order | delegated-not-ratified | S71 open |
| F16 chart / F17 CI root causes | flagged | byte-trace |
| Non-storm keyword/RECENCY profile | 3 storm runs only | passive |

## 5. TRAPS (<=8 lines)
- WINDOW ORDER CHANGED: FT-GAP-A -> SRC-INTEGRITY -> FT-GAP-B -> DET-DEAD (S70 delegated ruling).
- CHAT-DOC LANDING GATE (R-S70-2): wc -l vs expected BEFORE any commit of a saved doc; cat >> to an unsaved path silently creates a stub.
- HISTORY-BEFORE-RULING stands (memory #21); S70 proved it again 4x (Apr10/14, F4, Fox World, staging).
- Fed/DoE action-rec on /debate+/scenarios is FABRICATED — do not cite it as system output quality; GT-5 exhibit.
- Fox News World is LIVE and RITUAL-CLEAN (60) — never "clean up"; fossil lowercase "fox news" rows die in I-9 purge only.
- D stamp requires TABS (12 pages multi-view); a default-view screenshot is partial D.
- Swan citing score:0 articles is DESIGN (Stage-1 rejects = its hunting ground), not fabrication.
- Cron drift 2.3-3.5h standing; nominal+4h before missing. Quota 88% = storm cadence, known.

## 6. LOAD CHECK - next AI echoes EXACTLY these 5 lines, nothing more
HEAD = `8b03ed3` (ls-remote) + TREE CLEAN — 11 docs/copy commits S70, no shifter shipped, FT-GAP-A at clean #2
TOP3 = RULES-APPEND (R-S70-1/2), TRACE-READ evening run (final A-clean -> window: SRC-INTEGRITY first), F15-FAMILY relabel (4-page easy win)
DEADLINE = OC-A ~Jul 24 / CERT-before-model ~Aug 2 / Aug 9 keyfile / Groq cliff Aug 16 (31d, L-CLIFF Lens opener SOON + D-8 first move)
TRAP = window order now A->SRC-INTEGRITY->B->DET-DEAD; landing gate on chat docs (R-S70-2); Fed/DoE action-rec is fabricated (GT-5 exhibit A); D stamps need tabs
FIRST MOVE = ls-remote + git status (expect CLEAN); then RULES-APPEND: tail GNI_RULES.md, append R-S70-1/2, one docs commit

## 7. POINTERS (<=5 lines)
SRC arc: docs/SRC_INTEGRITY_DESIGN.md (I-1..I-9) + docs/SRC_REEXAM_S70.md (42 rows, R1-R4) — both committed.
CERT: docs/SUBPAGE_CERTIFICATION.md v2 (35 rows, tab scope, batch log) — the arc's cross-model memory.
Solver: mad_protocol.py:43 imports compute_depth; FLOOR_HIT/DRIFT prints there, never reaches mad_runner (D-7).
Writer B: credibility_model.py update_credibility_scores() <- main.py:252; last landed 2026-07-08 05:33:43.
Fabrication bytes: 2026-07-16 trace xlsx (354) + GROUNDING SHADOW line, MAD log L70.

---
RULES APPENDS for GNI_RULES.md (at S71 open, with landing gate):
R-S70-1: Shared DB state gets shared-route discipline (R-S55-1 kin): any table gaining a new writer
  triggers a WRITERS census — name every function writing each column. Two writers with different
  formulas for one column is a design review, not a merge; audit the seam, not the files.
R-S70-2: Chat-generated docs get a landing gate before first commit: wc -l on disk vs expected,
  STOP on mismatch. A cat >> to a never-saved path silently creates a stub —
  the D-8 disease in commit form.

DIARY S70 (<=10 lines):
The session where the audits audited us back. We opened planning one docs commit and closed
having built two ledgers — one for every source we trust, one for every page we show the world.
The weights table confessed to two writers fighting over one column for months; the debate
confessed to inventing a federal denial and publishing advice about it; and our own certification
ledger confessed to being a ten-line stub wearing a full table's commit message. Fox News World,
suspect since March, walked free — scored, ritual-clean, sixty. The fallthrough gate fired its
second clean breath without being watched. Twenty-five debts opened the day; the log quietly
paid two of them by itself. Every confession got an instrument, every instrument got a commit.
We read the minutes before touching the walls — and this time, the minutes read us. 👊
