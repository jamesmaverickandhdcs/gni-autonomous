# GNI ARCHITECTURE — arc42 canvas

**Born S92, 2026-09-01. Amended S93. Structure: arc42 (12 sections). Sections 5, 6 and 7 are
GENERATED, never hand-written — see §5.**

> This document uses the structure of the arc42 architecture template, created by
> Dr. Peter Hruschka and Dr. Gernot Starke (arc42.org), licensed CC BY-SA 4.0.
> This derivative carries the same licence.

**WHY THIS FILE EXISTS.** GNI's four document types all record CHANGE: CONTRACT is
law, GNI_RULES is lessons, HANDOFF is delta, TARGET+ORDER is the queue. None of
them records what the system IS. Item 5.3 (S81) named this exactly — *"every doc
GNI has describes CHANGE; none describes the system as it IS, which is why every
session re-derives architecture by grepping"* — and S84 closed it without the
document ever being built. This is that document.

**ROUTING.** This file is neither law, nor lesson, nor state, nor queue. It is the
REFERENCE layer, which GNI has never had. It does not compete with the four homes;
it fills the gap they leave. Nothing here overrides CONTRACT.

---

## §1 INTRODUCTION & GOALS

**Mission (permanent, from CONTRACT):** autonomous geopolitical news intelligence,
transparent, bilingual, at zero cost.

**The three-layer vision, stated in the White Paper 2026-03-20 and unchanged:**

| Layer | Name | State on 2026-09-01 |
|-------|------|---------------------|
| 1 | Pipeline automation | **CURRENT** — 165 days here |
| 2 | Self-healing resilience | near-term goal |
| 3 | Full agentic autonomy | long-term goal |

The White Paper's own words for the destination: *the operator's role shifts from
technician to guardian — present to set boundaries, absent for routine operation.*

**Quality goal driving the current phase (TARGET):** TRUTHFULNESS OF OUTPUT —
what GNI says must be what GNI measured.

---

## §2 CONSTRAINTS

| Constraint | Consequence |
|---|---|
| $0/month, free tiers only (Groq, Cerebras, Supabase, Vercel, GitHub Actions) | quota is a first-class design input; 3 Groq accounts split the load |
| ONE operator, part-time, also running Project Lens, also a student | operator attention is the scarcest resource in the system |
| Repo is PRIVATE; chat sessions run with an empty container | chat cannot read the repo — every file arrives as an attachment (CONTRACT v6). **Does not apply to a local agent.** |
| Windows / Git Bash / MINGW64 | CRLF, BOM, and textconv hazards are real and recur |
| Operator pulls every git trigger | no unattended write path to main exists today |

---

## §3 CONTEXT & SCOPE

**External systems:** RSS/GDELT sources → Groq + Cerebras (inference) → Supabase
(state) → Vercel (public surface) → Telegram (delivery). GitHub Actions is the
scheduler and CI.

**Boundary:** GNI publishes to anyone; it authenticates nothing except its own
internal API key. The public surface is the product; the pipeline is the factory.

*(To be completed with a generated dependency list — see §5.)*

---

## §4 SOLUTION STRATEGY — which discipline governs what

Ruled by James at S92. Each framework has ONE home. A framework applied outside
its home is a routing error.

| Discipline | Governs | Why here, not elsewhere |
|---|---|---|
| **arc42** | this document's structure | the only free, proven template for "what the system IS"; 12 sections, tailorable |
| **PM** (scope baseline, definition of done, change control) | **the QUEUE only** | GNI is no longer a project; but the queue is, and unbounded queue growth is a scope-control failure |
| **SRE** (SLO, error budget, toil reduction, postmortem, runbook) | **the OPERATION** | the operation has no end date; "reduce toil through automation" is Layer 2 restated |
| **ITIL configuration management** | **the INVENTORY** — configuration items and their relationships | this is the Smart Desk: which secret feeds which workflow feeds which module |
| **ISO/IEC 14764** (corrective / adaptive / **perfective** / preventive) | **classifying every order item** | most items are perfective. Perfective is never urgent. Labelling alone re-ranks the queue |
| **DevOps** | **the pipeline from commit to production** | GNI has the Dev half (CI, CD, IaC, git discipline) and not the Ops half — no test runs in CI, no deploy verification, no feedback loop |
| **ITIL SLM** (service level management, external dependencies) | **anything that bears on a promise and is NOT ours to control** | the other six all assume the thing can be fixed by us; a platform scheduler cannot. Without a home, each new instance costs a session of digging before anyone can say "not ours, record it, move on" |

---

## §5 BUILDING BLOCK VIEW — **GENERATED**

**GENERATED by `tools/gni_blocks.py` from the AST of 80 tracked `*.py` at HEAD `f0b9ee5` — source manifest md5 `a9a6ca4ebaa10eeb3daba027aa163cba` (EOL-normalised, R-S98-3). This section carries NO clock of its own: re-running it on an unchanged tree reproduces these bytes exactly. Do not hand-edit — the next run overwrites it. If this section is absent, the generator failed and that absence is the signal.**

**Section 5 is what is WRITTEN. Section 6 is what ACTUALLY RAN; section 7 is what is DECLARED. Whether a workflow invokes a module directly is §7.1's entrypoint column, not this section's — see 5.6.**

### 5.0 The path roots this tree creates for itself

**29 modules call `sys.path.append` or `sys.path.insert`, 35 calls in total.** This is not a footnote: it is why a single file answers to several names. `ai_engine/mad_runner.py` puts its own directory on the path, so its siblings import each other as `from quota_guard import ...` and its subpackages as `from analysis.mad_protocol import ...` — never by the `ai_engine.` prefix a package-relative reader would expect. Every name below is resolved against every root the tree creates, and a name claimed by two files produces NO edge.

| module | `sys.path` calls |
|---|---|
| `ai_engine.tests.dryrun_rate_governor` | 2 |
| `ai_engine.tests.dryrun_nn5_gate` | 2 |
| `ai_engine.tests.dryrun_mad_redefinition` | 2 |
| `ai_engine.tests.dryrun_false_neutral` | 2 |
| `ai_engine.mad_runner` | 2 |
| `ai_engine.analysis.supabase_saver` | 2 |
| `replay_scorer` | 1 |
| `pentest_injection` | 1 |
| `mad_model_probe` | 1 |
| `dryrun_two_account_split` | 1 |
| `design_bench` | 1 |
| `ai_engine.tests.test_token_accumulator` | 1 |
| `ai_engine.tests.test_injection_normalize` | 1 |
| `ai_engine.tests.test_groq_guardian` | 1 |
| `ai_engine.tests.test_dedup_novelty` | 1 |
| `ai_engine.tests.test_analysis_guardian` | 1 |
| `ai_engine.self_healing_runner` | 1 |
| `ai_engine.monitoring_pipeline` | 1 |
| `ai_engine.main` | 1 |
| `ai_engine.mad_preflight` | 1 |
| `ai_engine.funnel.intelligence_funnel` | 1 |
| `ai_engine.analysis.source_weights` | 1 |
| `ai_engine.analysis.self_bias_gate` | 1 |
| `ai_engine.analysis.outcome_verifier` | 1 |
| `ai_engine.analysis.nexus_analyzer` | 1 |
| `ai_engine.analysis.keyword_sensor` | 1 |
| `ai_engine.analysis.gni_graph_job` | 1 |
| `ai_engine.analysis.credibility_model` | 1 |
| `ai_engine.adaptive_pipeline` | 1 |

**Ambiguous names: 0.** No name is claimed by two files.

### 5.1 Module inventory by package

| package | modules | internal imports out | imported by |
|---|---|---|---|
| `(repo root)` | 3 | 2 | 0 |
| `ai_engine` | 12 | 40 | 16 |
| `ai_engine/analysis` | 33 | 21 | 52 |
| `ai_engine/collectors` | 3 | 1 | 5 |
| `ai_engine/funnel` | 2 | 2 | 6 |
| `ai_engine/geo` | 2 | 0 | 1 |
| `ai_engine/notifications` | 2 | 0 | 2 |
| `ai_engine/tests` | 10 | 11 | 0 |
| `scripts` | 1 | 0 | 0 |
| `tests` | 1 | 1 | 0 |
| `tools` | 11 | 7 | 3 |

### 5.2 What each module imports, and who imports it

Internal edges only. An import that resolves to no module in the tree is EXTERNAL and counted in the last column; this generator deliberately does not split stdlib from third-party, because doing so needs a hand-written list and a hand-written list is what C5 forbids.

| module | file | imports (internal) | imported by | external |
|---|---|---|---|---|
| `ai_engine` | `ai_engine/__init__.py` | — | **none** | 0 |
| `ai_engine.adaptive_pipeline` | `ai_engine/adaptive_pipeline.py` | `ai_engine.analysis.escalation_scorer`, `ai_engine.analysis.nexus_analyzer`, `ai_engine.analysis.supabase_saver`, `ai_engine.collectors.rss_collector`, `ai_engine.funnel.intelligence_funnel`, `ai_engine.monitoring_pipeline`, `ai_engine.quota_guard` | **none** | 6 |
| `ai_engine.analysis` | `ai_engine/analysis/__init__.py` | — | **none** | 0 |
| `ai_engine.analysis.absence_detector` | `ai_engine/analysis/absence_detector.py` | — | `ai_engine.main` | 4 |
| `ai_engine.analysis.audit_trail` | `ai_engine/analysis/audit_trail.py` | — | `ai_engine.analysis.self_bias_gate`, `ai_engine.main`, `ai_engine.self_healing_runner` | 6 |
| `ai_engine.analysis.code_fix_suggester` | `ai_engine/analysis/code_fix_suggester.py` | — | `ai_engine.main` | 5 |
| `ai_engine.analysis.credibility_model` | `ai_engine/analysis/credibility_model.py` | `ai_engine.analysis.source_weights` | `ai_engine.main` | 6 |
| `ai_engine.analysis.debate_prediction_verifier` | `ai_engine/analysis/debate_prediction_verifier.py` | — | **none** | 9 |
| `ai_engine.analysis.deception_detector` | `ai_engine/analysis/deception_detector.py` | — | `ai_engine.main` | 2 |
| `ai_engine.analysis.entity_extractor` | `ai_engine/analysis/entity_extractor.py` | — | `ai_engine.analysis.supabase_saver` | 1 |
| `ai_engine.analysis.entity_graph` | `ai_engine/analysis/entity_graph.py` | — | `ai_engine.analysis.gni_graph_job` | 2 |
| `ai_engine.analysis.escalation_scorer` | `ai_engine/analysis/escalation_scorer.py` | — | `ai_engine.adaptive_pipeline`, `ai_engine.main`, `design_bench`, `replay_scorer` | 0 |
| `ai_engine.analysis.frequency_controller` | `ai_engine/analysis/frequency_controller.py` | — | `ai_engine.main` | 4 |
| `ai_engine.analysis.gni_forensic_trace` | `ai_engine/analysis/gni_forensic_trace.py` | — | `ai_engine.main` | 8 |
| `ai_engine.analysis.gni_graph_job` | `ai_engine/analysis/gni_graph_job.py` | `ai_engine.analysis.entity_graph`, `ai_engine.analysis.supabase_saver` | **none** | 3 |
| `ai_engine.analysis.grounding_watch` | `ai_engine/analysis/grounding_watch.py` | — | **none** | 7 |
| `ai_engine.analysis.health_agent` | `ai_engine/analysis/health_agent.py` | — | `ai_engine.main`, `ai_engine.self_healing_runner` | 4 |
| `ai_engine.analysis.historical_correlations` | `ai_engine/analysis/historical_correlations.py` | — | `ai_engine.main` | 4 |
| `ai_engine.analysis.keyword_sensor` | `ai_engine/analysis/keyword_sensor.py` | `ai_engine.funnel.intelligence_funnel`, `ai_engine.matching` | **none** | 11 |
| `ai_engine.analysis.llm_health_probe` | `ai_engine/analysis/llm_health_probe.py` | — | `ai_engine.main`, `ai_engine.self_healing_runner` | 4 |
| `ai_engine.analysis.mad_budget_solver` | `ai_engine/analysis/mad_budget_solver.py` | — | `ai_engine.analysis.mad_protocol` | 1 |
| `ai_engine.analysis.mad_grounding_gate` | `ai_engine/analysis/mad_grounding_gate.py` | — | `ai_engine.analysis.mad_protocol`, `ai_engine.mad_runner` | 2 |
| `ai_engine.analysis.mad_protocol` | `ai_engine/analysis/mad_protocol.py` | `ai_engine.analysis.mad_budget_solver`, `ai_engine.analysis.mad_grounding_gate`, `ai_engine.groq_guardian`, `ai_engine.mad_rate_governor` | `ai_engine.mad_runner`, `ai_engine.tests.dryrun_false_neutral`, `ai_engine.tests.dryrun_mad_redefinition`, `ai_engine.tests.dryrun_nn5_gate`, `ai_engine.tests.dryrun_rate_governor`, `mad_model_probe` | 7 |
| `ai_engine.analysis.mad_quality` | `ai_engine/analysis/mad_quality.py` | — | `ai_engine.mad_runner` | 3 |
| `ai_engine.analysis.nexus_analyzer` | `ai_engine/analysis/nexus_analyzer.py` | `ai_engine.collectors.rss_collector`, `ai_engine.funnel.intelligence_funnel`, `ai_engine.groq_guardian` | `ai_engine.adaptive_pipeline`, `ai_engine.main`, `ai_engine.tests.test_token_accumulator` | 7 |
| `ai_engine.analysis.outcome_verifier` | `ai_engine/analysis/outcome_verifier.py` | `ai_engine.analysis.source_weights`, `ai_engine.analysis.supabase_saver`, `ai_engine.collectors.alpha_vantage`, `ai_engine.notifications.telegram_notifier` | **none** | 6 |
| `ai_engine.analysis.prompt_manager` | `ai_engine/analysis/prompt_manager.py` | — | `ai_engine.main` | 4 |
| `ai_engine.analysis.quality_scorer` | `ai_engine/analysis/quality_scorer.py` | — | `ai_engine.main` | 3 |
| `ai_engine.analysis.self_bias_gate` | `ai_engine/analysis/self_bias_gate.py` | `ai_engine.analysis.audit_trail` | **none** | 5 |
| `ai_engine.analysis.semantic_validator` | `ai_engine/analysis/semantic_validator.py` | — | `ai_engine.main` | 0 |
| `ai_engine.analysis.source_health_monitor` | `ai_engine/analysis/source_health_monitor.py` | — | `ai_engine.collectors.rss_collector`, `ai_engine.main` | 6 |
| `ai_engine.analysis.source_weights` | `ai_engine/analysis/source_weights.py` | `ai_engine.analysis.supabase_saver`, `ai_engine.collectors.rss_collector` | `ai_engine.analysis.credibility_model`, `ai_engine.analysis.outcome_verifier`, `ai_engine.funnel.intelligence_funnel`, `ai_engine.main` | 4 |
| `ai_engine.analysis.staging_checker` | `ai_engine/analysis/staging_checker.py` | — | `ai_engine.main` | 3 |
| `ai_engine.analysis.supabase_saver` | `ai_engine/analysis/supabase_saver.py` | `ai_engine.analysis.entity_extractor`, `ai_engine.geo.geocoder` | `ai_engine.adaptive_pipeline`, `ai_engine.analysis.gni_graph_job`, `ai_engine.analysis.outcome_verifier`, `ai_engine.analysis.source_weights`, `ai_engine.main`, `ai_engine.tests.test_dedup_novelty`, `design_bench`, `replay_scorer` | 6 |
| `ai_engine.analysis.weekly_digest` | `ai_engine/analysis/weekly_digest.py` | — | `ai_engine.main` | 6 |
| `ai_engine.collectors` | `ai_engine/collectors/__init__.py` | — | **none** | 0 |
| `ai_engine.collectors.alpha_vantage` | `ai_engine/collectors/alpha_vantage.py` | — | `ai_engine.analysis.outcome_verifier` | 4 |
| `ai_engine.collectors.rss_collector` | `ai_engine/collectors/rss_collector.py` | `ai_engine.analysis.source_health_monitor` | `ai_engine.adaptive_pipeline`, `ai_engine.analysis.nexus_analyzer`, `ai_engine.analysis.source_weights`, `ai_engine.main` | 4 |
| `ai_engine.fetch_prices` | `ai_engine/fetch_prices.py` | — | **none** | 6 |
| `ai_engine.funnel` | `ai_engine/funnel/__init__.py` | — | **none** | 0 |
| `ai_engine.funnel.intelligence_funnel` | `ai_engine/funnel/intelligence_funnel.py` | `ai_engine.analysis.source_weights`, `ai_engine.matching` | `ai_engine.adaptive_pipeline`, `ai_engine.analysis.keyword_sensor`, `ai_engine.analysis.nexus_analyzer`, `ai_engine.main`, `ai_engine.tests.test_injection_normalize`, `pentest_injection` | 10 |
| `ai_engine.geo` | `ai_engine/geo/__init__.py` | — | **none** | 0 |
| `ai_engine.geo.geocoder` | `ai_engine/geo/geocoder.py` | — | `ai_engine.analysis.supabase_saver` | 6 |
| `ai_engine.groq_guardian` | `ai_engine/groq_guardian.py` | — | `ai_engine.analysis.mad_protocol`, `ai_engine.analysis.nexus_analyzer`, `ai_engine.tests.test_analysis_guardian`, `ai_engine.tests.test_groq_guardian` | 3 |
| `ai_engine.mad_preflight` | `ai_engine/mad_preflight.py` | — | `ai_engine.mad_runner` | 5 |
| `ai_engine.mad_rate_governor` | `ai_engine/mad_rate_governor.py` | — | `ai_engine.analysis.mad_protocol`, `ai_engine.tests.dryrun_rate_governor` | 2 |
| `ai_engine.mad_runner` | `ai_engine/mad_runner.py` | `ai_engine.analysis.mad_grounding_gate`, `ai_engine.analysis.mad_protocol`, `ai_engine.analysis.mad_quality`, `ai_engine.mad_preflight`, `ai_engine.quota_guard` | `ai_engine.tests.dryrun_false_neutral` | 7 |
| `ai_engine.main` | `ai_engine/main.py` | `ai_engine.analysis.absence_detector`, `ai_engine.analysis.audit_trail`, `ai_engine.analysis.code_fix_suggester`, `ai_engine.analysis.credibility_model`, `ai_engine.analysis.deception_detector`, `ai_engine.analysis.escalation_scorer`, `ai_engine.analysis.frequency_controller`, `ai_engine.analysis.gni_forensic_trace`, `ai_engine.analysis.health_agent`, `ai_engine.analysis.historical_correlations`, `ai_engine.analysis.llm_health_probe`, `ai_engine.analysis.nexus_analyzer`, `ai_engine.analysis.prompt_manager`, `ai_engine.analysis.quality_scorer`, `ai_engine.analysis.semantic_validator`, `ai_engine.analysis.source_health_monitor`, `ai_engine.analysis.source_weights`, `ai_engine.analysis.staging_checker`, `ai_engine.analysis.supabase_saver`, `ai_engine.analysis.weekly_digest`, `ai_engine.collectors.rss_collector`, `ai_engine.funnel.intelligence_funnel`, `ai_engine.notifications.telegram_notifier`, `ai_engine.quota_guard` | `ai_engine.self_healing_runner` | 6 |
| `ai_engine.matching` | `ai_engine/matching.py` | — | `ai_engine.analysis.keyword_sensor`, `ai_engine.funnel.intelligence_funnel` | 2 |
| `ai_engine.monitoring_pipeline` | `ai_engine/monitoring_pipeline.py` | — | `ai_engine.adaptive_pipeline` | 7 |
| `ai_engine.notifications` | `ai_engine/notifications/__init__.py` | — | **none** | 0 |
| `ai_engine.notifications.telegram_notifier` | `ai_engine/notifications/telegram_notifier.py` | — | `ai_engine.analysis.outcome_verifier`, `ai_engine.main` | 5 |
| `ai_engine.quota_guard` | `ai_engine/quota_guard.py` | — | `ai_engine.adaptive_pipeline`, `ai_engine.mad_runner`, `ai_engine.main`, `dryrun_two_account_split` | 6 |
| `ai_engine.self_healing_runner` | `ai_engine/self_healing_runner.py` | `ai_engine.analysis.audit_trail`, `ai_engine.analysis.health_agent`, `ai_engine.analysis.llm_health_probe`, `ai_engine.main` | **none** | 5 |
| `ai_engine.tests` | `ai_engine/tests/__init__.py` | — | **none** | 0 |
| `ai_engine.tests.dryrun_false_neutral` | `ai_engine/tests/dryrun_false_neutral.py` | `ai_engine.analysis.mad_protocol`, `ai_engine.mad_runner` | **none** | 4 |
| `ai_engine.tests.dryrun_mad_redefinition` | `ai_engine/tests/dryrun_mad_redefinition.py` | `ai_engine.analysis.mad_protocol` | **none** | 2 |
| `ai_engine.tests.dryrun_nn5_gate` | `ai_engine/tests/dryrun_nn5_gate.py` | `ai_engine.analysis.mad_protocol` | **none** | 4 |
| `ai_engine.tests.dryrun_rate_governor` | `ai_engine/tests/dryrun_rate_governor.py` | `ai_engine.analysis.mad_protocol`, `ai_engine.mad_rate_governor` | **none** | 2 |
| `ai_engine.tests.test_analysis_guardian` | `ai_engine/tests/test_analysis_guardian.py` | `ai_engine.groq_guardian` | **none** | 2 |
| `ai_engine.tests.test_dedup_novelty` | `ai_engine/tests/test_dedup_novelty.py` | `ai_engine.analysis.supabase_saver` | **none** | 2 |
| `ai_engine.tests.test_groq_guardian` | `ai_engine/tests/test_groq_guardian.py` | `ai_engine.groq_guardian` | **none** | 2 |
| `ai_engine.tests.test_injection_normalize` | `ai_engine/tests/test_injection_normalize.py` | `ai_engine.funnel.intelligence_funnel` | **none** | 3 |
| `ai_engine.tests.test_token_accumulator` | `ai_engine/tests/test_token_accumulator.py` | `ai_engine.analysis.nexus_analyzer` | **none** | 2 |
| `design_bench` | `tools/design_bench.py` | `ai_engine.analysis.escalation_scorer`, `ai_engine.analysis.supabase_saver` | **none** | 3 |
| `dryrun_two_account_split` | `dryrun_two_account_split.py` | `ai_engine.quota_guard` | **none** | 2 |
| `gni_blocks` | `tools/gni_blocks.py` | — | **none** | 9 |
| `gni_macro_map` | `tools/gni_macro_map.py` | — | `gni_rule_checks`, `gni_rule_checks_fixture` | 4 |
| `gni_rule_checks` | `tools/gni_rule_checks.py` | `gni_macro_map`, `gni_runtime` | **none** | 6 |
| `gni_rule_checks_fixture` | `tools/gni_rule_checks_fixture.py` | `gni_macro_map` | **none** | 5 |
| `gni_runtime` | `tools/gni_runtime.py` | — | `gni_rule_checks` | 9 |
| `gni_state` | `tools/gni_state.py` | — | **none** | 7 |
| `mad_model_probe` | `mad_model_probe.py` | `ai_engine.analysis.mad_protocol` | **none** | 7 |
| `mission_control_check` | `scripts/mission_control_check.py` | — | **none** | 2 |
| `mk_order_s95` | `tools/mk_order_s95.py` | — | **none** | 2 |
| `mk_rules_s95_append` | `tools/mk_rules_s95_append.py` | — | **none** | 2 |
| `mk_rules_s95_markers` | `tools/mk_rules_s95_markers.py` | — | **none** | 3 |
| `patch_arch_s99` | `patch_arch_s99.py` | — | **none** | 1 |
| `pentest_injection` | `tests/pentest_injection.py` | `ai_engine.funnel.intelligence_funnel` | **none** | 2 |
| `replay_scorer` | `tools/replay_scorer.py` | `ai_engine.analysis.escalation_scorer`, `ai_engine.analysis.supabase_saver` | **none** | 3 |

### 5.3 Modules with NO STATIC REFERENCE FOUND

Nothing in the tree imports these, and they are not test modules. **This is not a deletion list.** A module can be reached by a workflow entrypoint, by `python -m`, or by a name built at run time — none of which an AST sees. Read this beside §7.1's entrypoint column before concluding anything.

| module | file | in §7.1's entrypoint column? |
|---|---|---|
| `ai_engine.adaptive_pipeline` | `ai_engine/adaptive_pipeline.py` | *check §7.1* |
| `ai_engine.analysis.debate_prediction_verifier` | `ai_engine/analysis/debate_prediction_verifier.py` | *check §7.1* |
| `ai_engine.analysis.gni_graph_job` | `ai_engine/analysis/gni_graph_job.py` | *check §7.1* |
| `ai_engine.analysis.grounding_watch` | `ai_engine/analysis/grounding_watch.py` | *check §7.1* |
| `ai_engine.analysis.keyword_sensor` | `ai_engine/analysis/keyword_sensor.py` | *check §7.1* |
| `ai_engine.analysis.outcome_verifier` | `ai_engine/analysis/outcome_verifier.py` | *check §7.1* |
| `ai_engine.analysis.self_bias_gate` | `ai_engine/analysis/self_bias_gate.py` | *check §7.1* |
| `ai_engine.fetch_prices` | `ai_engine/fetch_prices.py` | *check §7.1* |
| `ai_engine.self_healing_runner` | `ai_engine/self_healing_runner.py` | *check §7.1* |
| `design_bench` | `tools/design_bench.py` | *check §7.1* |
| `dryrun_two_account_split` | `dryrun_two_account_split.py` | *check §7.1* |
| `gni_blocks` | `tools/gni_blocks.py` | *check §7.1* |
| `gni_rule_checks` | `tools/gni_rule_checks.py` | *check §7.1* |
| `gni_rule_checks_fixture` | `tools/gni_rule_checks_fixture.py` | *check §7.1* |
| `gni_state` | `tools/gni_state.py` | *check §7.1* |
| `mad_model_probe` | `mad_model_probe.py` | *check §7.1* |
| `mission_control_check` | `scripts/mission_control_check.py` | *check §7.1* |
| `mk_order_s95` | `tools/mk_order_s95.py` | *check §7.1* |
| `mk_rules_s95_append` | `tools/mk_rules_s95_append.py` | *check §7.1* |
| `mk_rules_s95_markers` | `tools/mk_rules_s95_markers.py` | *check §7.1* |
| `patch_arch_s99` | `patch_arch_s99.py` | *check §7.1* |
| `replay_scorer` | `tools/replay_scorer.py` | *check §7.1* |

### 5.4 Module-level symbols with NO STATIC REFERENCE FOUND ANYWHERE

**35 of 973** module-level symbols (dunders excluded). A symbol here is defined at module level and its name is READ nowhere in the tree — not by another module and not by its own. This is the class that produced DET-DEAD and `_FORCE_PROVIDER`, five months apart, both found by accident.

A further **636** are read only inside the module that defines them. Those are private helpers, not candidates, and are not listed. An earlier draft of this generator counted them here and the table came out at 27 of 31 rows — a true statement with no signal in it.

| module | symbols |
|---|---|
| `ai_engine.analysis.audit_trail` | `get_recent_audit_events` |
| `ai_engine.analysis.gni_forensic_trace` | `C_GRAY`, `C_GRAY_FG`, `_cell_result`, `_row_bg` |
| `ai_engine.analysis.health_agent` | `get_unresolved_alerts` |
| `ai_engine.analysis.keyword_sensor` | `_suggest_pillar`, `run_keyword_sensor` |
| `ai_engine.analysis.outcome_verifier` | `fetch_price_change` |
| `ai_engine.analysis.prompt_manager` | `update_mad_confidence` |
| `ai_engine.analysis.quality_scorer` | `KNOWN_SENTIMENTS`, `KNOWN_TICKERS` |
| `ai_engine.analysis.semantic_validator` | `BEARISH_RISK_LEVELS` |
| `ai_engine.analysis.source_health_monitor` | `MIN_EXPECTED` |
| `ai_engine.analysis.source_weights` | `_cache_loaded_at`, `get_weights_summary` |
| `ai_engine.analysis.weekly_digest` | `get_latest_digest` |
| `ai_engine.geo.geocoder` | `extract_locations` |
| `ai_engine.quota_guard` | `PIPELINE_RESERVATIONS` |
| `ai_engine.tests.dryrun_mad_redefinition` | `ALL_AGENT_CONS`, `ALL_ELEVEN` |
| `ai_engine.tests.dryrun_rate_governor` | `APITimeoutError` |
| `ai_engine.tests.test_dedup_novelty` | `test_new_basket_publishes`, `test_old_shape_witness_does_not_skip`, `test_recycled_basket_skips` |
| `ai_engine.tests.test_groq_guardian` | `TestInjectionAttempt`, `TestQualityResponse`, `TestRateLimitError`, `TestRefusal`, `TestValidateResponse` |
| `ai_engine.tests.test_injection_normalize` | `test_evasion_caught_after_normalize`, `test_html_hex_entity_attack_still_blocked`, `test_negative_stays_pass`, `test_regression_still_caught` |
| `mk_rules_s95_markers` | `MARK` |

### 5.5 Which modules a test module imports

**11 test modules import 7 of the 69 non-test modules.** Coverage here means ONE THING ONLY: a test module names it in an import. It does not mean the module's behaviour is asserted, and it does not mean the test passes — 36 assertions in `ai_engine/tests/` have never been run in CI (order item).

| test module | imports |
|---|---|
| `ai_engine.tests` | — |
| `ai_engine.tests.dryrun_false_neutral` | `ai_engine.analysis.mad_protocol`, `ai_engine.mad_runner` |
| `ai_engine.tests.dryrun_mad_redefinition` | `ai_engine.analysis.mad_protocol` |
| `ai_engine.tests.dryrun_nn5_gate` | `ai_engine.analysis.mad_protocol` |
| `ai_engine.tests.dryrun_rate_governor` | `ai_engine.analysis.mad_protocol`, `ai_engine.mad_rate_governor` |
| `ai_engine.tests.test_analysis_guardian` | `ai_engine.groq_guardian` |
| `ai_engine.tests.test_dedup_novelty` | `ai_engine.analysis.supabase_saver` |
| `ai_engine.tests.test_groq_guardian` | `ai_engine.groq_guardian` |
| `ai_engine.tests.test_injection_normalize` | `ai_engine.funnel.intelligence_funnel` |
| `ai_engine.tests.test_token_accumulator` | `ai_engine.analysis.nexus_analyzer` |
| `pentest_injection` | `ai_engine.funnel.intelligence_funnel` |

**Imported by no test module (62):** `ai_engine`, `ai_engine.adaptive_pipeline`, `ai_engine.analysis`, `ai_engine.analysis.absence_detector`, `ai_engine.analysis.audit_trail`, `ai_engine.analysis.code_fix_suggester`, `ai_engine.analysis.credibility_model`, `ai_engine.analysis.debate_prediction_verifier`, `ai_engine.analysis.deception_detector`, `ai_engine.analysis.entity_extractor`, `ai_engine.analysis.entity_graph`, `ai_engine.analysis.escalation_scorer`, `ai_engine.analysis.frequency_controller`, `ai_engine.analysis.gni_forensic_trace`, `ai_engine.analysis.gni_graph_job`, `ai_engine.analysis.grounding_watch`, `ai_engine.analysis.health_agent`, `ai_engine.analysis.historical_correlations`, `ai_engine.analysis.keyword_sensor`, `ai_engine.analysis.llm_health_probe`, `ai_engine.analysis.mad_budget_solver`, `ai_engine.analysis.mad_grounding_gate`, `ai_engine.analysis.mad_quality`, `ai_engine.analysis.outcome_verifier`, `ai_engine.analysis.prompt_manager`, `ai_engine.analysis.quality_scorer`, `ai_engine.analysis.self_bias_gate`, `ai_engine.analysis.semantic_validator`, `ai_engine.analysis.source_health_monitor`, `ai_engine.analysis.source_weights`, `ai_engine.analysis.staging_checker`, `ai_engine.analysis.weekly_digest`, `ai_engine.collectors`, `ai_engine.collectors.alpha_vantage`, `ai_engine.collectors.rss_collector`, `ai_engine.fetch_prices`, `ai_engine.funnel`, `ai_engine.geo`, `ai_engine.geo.geocoder`, `ai_engine.mad_preflight`, `ai_engine.main`, `ai_engine.matching`, `ai_engine.monitoring_pipeline`, `ai_engine.notifications`, `ai_engine.notifications.telegram_notifier`, `ai_engine.quota_guard`, `ai_engine.self_healing_runner`, `design_bench`, `dryrun_two_account_split`, `gni_blocks`, `gni_macro_map`, `gni_rule_checks`, `gni_rule_checks_fixture`, `gni_runtime`, `gni_state`, `mad_model_probe`, `mission_control_check`, `mk_order_s95`, `mk_rules_s95_append`, `mk_rules_s95_markers`, `patch_arch_s99`, `replay_scorer`

### 5.6 What §5 does not answer

- **Whether an import is CALLED.** This is a static reader. `imports X` is not `calls X at run time`; §6 holds the runtime side.
- **Dynamic reference.** `getattr`, a name assembled from a string, a plugin registry, an `__all__` re-export and a CLI entrypoint are all invisible here. Every column above therefore says NO STATIC REFERENCE FOUND and never `dead`.
- **Same-name collisions mask a lonely symbol.** 5.4 matches symbol names across the whole tree, so a symbol called `main` is never reported even when this module's `main` is unused. The failure direction is a FALSE NEGATIVE — the section under-reports rather than accusing live code — and that is deliberate.
- **Whether a workflow runs a module.** §7.1's entrypoint column. Joining the two tables is the reader's job and both halves stay true; this generator does not parse YAML.
- **pytest discovery.** `test_*` functions and `Test*` classes are found by NAME at run time, so 5.4 lists them as unreferenced. They are collected by the runner, not called by any module. Read that block with this in mind.
- **Untracked files.** The inventory is `git ls-files '*.py'`. A file that is not tracked is not architecture.
## §6 RUNTIME VIEW — **GENERATED**

**GENERATED by `tools/gni_runtime.py` from `docs/gni_runtime_snapshot_S99.json` — harvested 2026-09-04T07:29:21Z at HEAD `3b088a6`, snapshot md5 `f2f21786fe0f2448ab35af178526f56b` (EOL-normalised, R-S98-3). Window: 2026-08-19 to 2026-09-03 UTC, 16 complete days. This section carries NO clock of its own: re-rendering an unchanged snapshot reproduces these bytes exactly. Do not hand-edit — the next run overwrites it. If this section is absent, the generator failed and that absence is the signal.**

**Section 6 is what ACTUALLY RAN. Section 7 is what is DECLARED; section 5 is what is WRITTEN. "What calls what" is section 5's question, not this one.**

### 6.1 Declared cadence vs delivered runs

Scheduled runs only — `workflow_dispatch` and `push` are excluded, because a manual run is not evidence about a schedule.

| workflow | cron (nominal UTC) | declared slots | delivered | window delivery | per-day min · median · max |
|---|---|---|---|---|---|
| `gni_heartbeat.yml` | `0,30 * * * *` | 768 | 282 | **37%** | 4% · 38% · 77% |
| `gni_selfcheck.yml` | `*/30 * * * *` | 768 | 292 | **38%** | 4% · 38% · 81% |
| `gni_graph.yml` | `58 2 * * *`<br>`58 10 * * *` | 32 | 32 | **100%** | 100% · 100% · 100% |
| `gni_mad.yml` | `43 2 * * *`<br>`43 10 * * *`<br>`13 11 * * *` | 48 | 48 | **100%** | 100% · 100% · 100% |
| `gni_market.yml` | `0 14 * * 1-5`<br>`0 15 * * 1-5`<br>`0 16 * * 1-5`<br>`0 17 * * 1-5`<br>`0 18 * * 1-5`<br>`0 19 * * 1-5`<br>`0 20 * * 1-5` | 84 | 84 | **100%** | 100% · 100% · 100% |
| `gni_pipeline.yml` | `13 2 * * *`<br>`13 10 * * *` | 32 | 32 | **100%** | 100% · 100% · 100% |
| `gni_selfbias.yml` | `0 6 * * *` | 16 | 16 | **100%** | 100% · 100% · 100% |

**Total: 786 of 1748 declared slots delivered (45%) across 16 complete days.**

**The per-day spread sits beside every window figure, and it is not decoration.** A window ratio is an average, and DECISION S90-3 put average-across-a-boundary in the GRAVEYARD after a published token figure proved reproducible from no window at all. Where min and max sit far apart the window figure describes no day that happened, and 6.4 is the row-by-row record.

**Attribution.** A run is evidence about the SLOT it served, not about the day it started on. Where every declared slot produced a run the slot is known exactly and the per-day figures are attributed to it. Where slots are missing no pairing exists, and the creation day is used as a fallback: `gni_heartbeat.yml`, `gni_selfcheck.yml`. Under lateness a run can then cross midnight, so a fallback day can borrow a run from its neighbour; a column marked ⚠ exceeded 100% on at least one day for that reason and is a property of the attribution, not of the system. Partial days at both ends of the window are dropped for the same reason.

### 6.2 Slot lateness — published only where it CAN be measured

Lateness is measured against the SLOT, never against the previous run (R-S87-6, second amendment) — and it is published **only where delivery was complete**. When every declared slot produced a run, the k-th run is the k-th slot and the lag is exact at any size. When slots are missing, no pairing is defensible and this section prints the reason instead of a number.

**The instrument that was REJECTED, and why, because it is the obvious one:** "distance back to the nearest declared slot at or before the run". On a 30-minute cron a slot is never more than 30 minutes behind anything, so a run 365 minutes late reads as **5 minutes** late. That method is bounded by the very spacing it is supposed to be independent of, and it returns its smallest possible answer exactly where the truth is largest. The generator's control probe re-proves this on every run: it builds a 365-minute lag on a 30-minute cron and asserts the nearest-slot number understates it while ordered matching does not.

Derived band: **744 min** (12.4 h) — the maximum over the 5 workflow(s) with complete delivery: `gni_graph.yml`, `gni_mad.yml`, `gni_market.yml`, `gni_pipeline.yml`, `gni_selfbias.yml`. Compare against R-S87-6's recorded band; do not recall one.

| workflow | min slot spacing | lateness | median | p90 | max |
|---|---|---|---|---|---|
| `gni_graph.yml` | 480 min | measured | 226 min | 590 min | 703 min |
| `gni_heartbeat.yml` | 30 min | **NOT MEASURABLE** — delivered 282 of 768 slots -- runs cannot be paired to slots | — | — | — |
| `gni_mad.yml` | 30 min | measured | 226 min | 588 min | 712 min |
| `gni_market.yml` | 60 min | measured | 130 min | 475 min | 556 min |
| `gni_pipeline.yml` | 480 min | measured | 255 min | 616 min | 727 min |
| `gni_selfbias.yml` | 1440 min | measured | 296 min | 689 min | 744 min |
| `gni_selfcheck.yml` | 30 min | **NOT MEASURABLE** — delivered 292 of 768 slots -- runs cannot be paired to slots | — | — | — |

### 6.3 Outcome distribution of the runs that DID happen

| workflow | success |
|---|---|
| `gni_graph.yml` | 32 |
| `gni_heartbeat.yml` | 282 |
| `gni_mad.yml` | 48 |
| `gni_market.yml` | 84 |
| `gni_pipeline.yml` | 32 |
| `gni_selfbias.yml` | 16 |
| `gni_selfcheck.yml` | 292 |

**A green conclusion says the job exited zero. It does not say the slot fired, and 6.1 is where that question is answered.**

### 6.4 Delivery by day

| day | `gni_graph` | `gni_heartbeat` | `gni_mad` | `gni_market` | `gni_pipeline` | `gni_selfbias` | `gni_selfcheck` |
|---|---|---|---|---|---|---|---|
| 2026-08-19 | 2/2 | 33/48 | 3/3 | 7/7 | 2/2 | 1/1 | 33/48 |
| 2026-08-20 | 2/2 | 30/48 | 3/3 | 7/7 | 2/2 | 1/1 | 32/48 |
| 2026-08-21 | 2/2 | 30/48 | 3/3 | 7/7 | 2/2 | 1/1 | 31/48 |
| 2026-08-22 | 2/2 | 37/48 | 3/3 | — | 2/2 | 1/1 | 39/48 |
| 2026-08-23 | 2/2 | 37/48 | 3/3 | — | 2/2 | 1/1 | 38/48 |
| 2026-08-24 | 2/2 | 28/48 | 3/3 | 7/7 | 2/2 | 1/1 | 29/48 |
| 2026-08-25 | 2/2 | 28/48 | 3/3 | 7/7 | 2/2 | 1/1 | 31/48 |
| 2026-08-26 | 2/2 | 18/48 | 3/3 | 7/7 | 2/2 | 1/1 | 18/48 |
| 2026-08-27 | 2/2 | 3/48 | 3/3 | 7/7 | 2/2 | 1/1 | 3/48 |
| 2026-08-28 | 2/2 | 2/48 | 3/3 | 7/7 | 2/2 | 1/1 | 2/48 |
| 2026-08-29 | 2/2 | 6/48 | 3/3 | — | 2/2 | 1/1 | 6/48 |
| 2026-08-30 | 2/2 | 6/48 | 3/3 | — | 2/2 | 1/1 | 6/48 |
| 2026-08-31 | 2/2 | 4/48 | 3/3 | 7/7 | 2/2 | 1/1 | 4/48 |
| 2026-09-01 | 2/2 | 6/48 | 3/3 | 7/7 | 2/2 | 1/1 | 7/48 |
| 2026-09-02 | 2/2 | 7/48 | 3/3 | 7/7 | 2/2 | 1/1 | 6/48 |
| 2026-09-03 | 2/2 | 7/48 | 3/3 | 7/7 | 2/2 | 1/1 | 7/48 |

### 6.5 What §6 does not answer

- **Why** a slot did not fire. This section measures delivery; it does not diagnose the scheduler.
- **Job level.** `gni_mad.yml` carries both the debate and the grounding-watch and they are separable only by reading each run's jobs (one API call per run). This generator is workflow-level by design; the job-level split is filed, not silently skipped.
- **What calls what.** Section 5.
- **A known edge, disclosed here at ship time rather than found later.** The pairing in 6.2 compares slots on complete days against runs created on those same days. When lateness is large enough to push a slot's run past midnight consistently, the two sets disagree at the window edges and the pairing REFUSES even though delivery was complete -- measured deliberately against a synthetic 12-hour uniform lag, which returns "a run BEFORE its slot at the window edge". The failure direction is safe: the section says NOT MEASURABLE instead of publishing a wrong lag. Widening the run collection past the window end without letting the previous day's runs leak in is the fix, and it is filed, not done.
- **Fetch truncation.** `gni_heartbeat.yml`, `gni_mad.yml`, `gni_market.yml`, `gni_pipeline.yml`, `gni_selfcheck.yml` hit the 300-run fetch limit. The window above is already bounded so that every workflow covers it, so the figures stand — but a longer window needs a larger limit.
## §7 DEPLOYMENT VIEW — **GENERATED**

**GENERATED by `tools/gni_state.py` from HEAD `f0b9ee5` — 9 workflow files, manifest md5 `0d7b557b9316047cc448ac5aa59f4431` (EOL-normalised, R-S98-3). This section carries NO CLOCK: re-running it on an unchanged tree reproduces these bytes exactly. Do not hand-edit: the next run overwrites it. If this section is absent, the generator failed and that absence is the signal.**

### 7.1 Workflow inventory

**9 workflows: 7 scheduled · 1 on push · 1 dispatch-only.** Cron times below are NOMINAL. Observed start times run hours later; the lateness band is measured, never recalled (R-S87-6 third amendment) and is NOT generated here — it belongs to §6.

| workflow | triggers | cron (nominal UTC) | jobs | entrypoint | secret refs |
|---|---|---|---|---|---|
| `gni_adaptive.yml` | workflow_dispatch | — | 1 | `python adaptive_pipeline.py` :52 | 8 |
| `gni_ci_harness.yml` | push, workflow_dispatch | — | 2 | `python tools/gni_rule_checks_fixture.py` :56<br>`python tools/gni_rule_checks.py` :59 | 0 |
| `gni_graph.yml` | schedule | `58 2 * * *`<br>`58 10 * * *` | 1 | `python -m analysis.gni_graph_job` :24 | 4 |
| `gni_heartbeat.yml` | schedule, workflow_dispatch | `0,30 * * * *` | 1 | `python monitoring_pipeline.py` :47 | 7 |
| `gni_mad.yml` | schedule, workflow_dispatch | `43 2 * * *`<br>`43 10 * * *`<br>`13 11 * * *` | 2 | `python ai_engine/mad_runner.py` :53<br>`python ai_engine/analysis/grounding_watch.py` :101 | 11 |
| `gni_market.yml` | schedule, workflow_dispatch | `0 14 * * 1-5`<br>`0 15 * * 1-5`<br>`0 16 * * 1-5`<br>`0 17 * * 1-5`<br>`0 18 * * 1-5`<br>`0 19 * * 1-5`<br>`0 20 * * 1-5` | 1 | `python ai_engine/fetch_prices.py` :47 | 4 |
| `gni_pipeline.yml` | schedule, workflow_dispatch | `13 2 * * *`<br>`13 10 * * *` | 2 | `python ai_engine/main.py` :36<br>`python ai_engine/analysis/outcome_verifier.py` :81<br>`python ai_engine/analysis/debate_prediction_verifier.py` :89 | 11 |
| `gni_selfbias.yml` | schedule | `0 6 * * *` | 1 | `python -m analysis.self_bias_gate` :28 | 4 |
| `gni_selfcheck.yml` | schedule | `*/30 * * * *` | 1 | `python3 scripts/mission_control_check.py` :18 | 4 |

### 7.2 The chain: secret → workflow (env alias) → code consumer

Stored secrets: 22 read from `gh secret list`. `GITHUB_TOKEN` is runner-provided, never stored.

| secret | stored | workflows | env alias(es) | code consumers | other files |
|---|---|---|---|---|---|
**Rows are the union of `gh secret list` and every `secrets.` reference in the workflows. A name that is neither stored nor referenced — an env var read only by code — is INVISIBLE HERE BY CONSTRUCTION. That blind spot is §5's job, not §7's.**

| `ALPHA_VANTAGE_API_KEY` | yes | `gni_pipeline.yml` | `ALPHA_VANTAGE_API_KEY` | 1 files, 1 hits — `tools/mk_order_s95.py` | 1 |
| `CEREBRAS_API_KEY` | yes | `gni_adaptive.yml` | `CEREBRAS_API_KEY` | 1 files, 4 hits — `ai_engine/analysis/nexus_analyzer.py` | 0 |
| `GITHUB_TOKEN` | runner | `gni_adaptive.yml`, `gni_heartbeat.yml` | `GITHUB_TOKEN` | 1 files, 2 hits — `ai_engine/monitoring_pipeline.py` | 0 |
| `GNI_APP_URL` | yes | `gni_selfcheck.yml` | `GNI_APP_URL` | 0 files, 0 hits | 0 |
| `GROQ_API_KEY` | yes | `gni_adaptive.yml`, `gni_heartbeat.yml`, `gni_mad.yml` | `GROQ_API_KEY` | 15 files, 38 hits — `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/keyword_sensor.py`, `ai_engine/analysis/llm_health_probe.py`, `ai_engine/analysis/mad_protocol.py`, `ai_engine/analysis/nexus_analyzer.py`, `ai_engine/analysis/weekly_digest.py` … | 27 |
| `GROQ_GNI_NOT_MAD` | yes | `gni_pipeline.yml` | `GROQ_API_KEY` | 15 files, 38 hits — `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/keyword_sensor.py`, `ai_engine/analysis/llm_health_probe.py`, `ai_engine/analysis/mad_protocol.py`, `ai_engine/analysis/nexus_analyzer.py`, `ai_engine/analysis/weekly_digest.py` … | 27 |
| `GROQ_MAD_EVENING` | yes | `gni_mad.yml` | — | 1 files, 1 hits — `tools/mk_order_s95.py` | 13 |
| `GROQ_MAD_MODEL` | yes | `gni_mad.yml` | `GROQ_MAD_MODEL` | 2 files, 8 hits — `ai_engine/analysis/mad_protocol.py`, `ai_engine/mad_preflight.py` | 17 |
| `GROQ_MODEL` | yes | `gni_mad.yml`, `gni_pipeline.yml` | `GROQ_MODEL` | 12 files, 30 hits — `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/keyword_sensor.py`, `ai_engine/analysis/llm_health_probe.py`, `ai_engine/analysis/mad_protocol.py`, `ai_engine/analysis/nexus_analyzer.py`, `ai_engine/analysis/weekly_digest.py` … | 35 |
| `GROQ_MODEL_FALLBACK` | yes | **none** | — | 4 files, 12 hits — `ai_engine/analysis/llm_health_probe.py`, `ai_engine/analysis/mad_protocol.py`, `ai_engine/analysis/nexus_analyzer.py`, `ai_engine/funnel/intelligence_funnel.py` | 31 |
| `GROQ_TEST_ONLY` | yes | **none** | — | 0 files, 0 hits | 7 |
| `MYANMAR_DISPATCH_PAT` | yes | `gni_mad.yml` | `MYANMAR_DISPATCH_PAT` | 1 files, 2 hits — `ai_engine/mad_runner.py` | 8 |
| `SELF_CHECK_INTERNAL_KEY` | yes | `gni_selfcheck.yml` | `SELF_CHECK_INTERNAL_KEY` | 2 files, 2 hits — `src/app/api/mission-control/route.ts`, `src/lib/auth.ts` | 0 |
| `SUPABASE_SERVICE_KEY` | yes | `gni_adaptive.yml`, `gni_graph.yml`, `gni_heartbeat.yml`, `gni_mad.yml`, `gni_market.yml`, `gni_pipeline.yml`, `gni_selfbias.yml` | `SUPABASE_SERVICE_KEY` | 50 files, 78 hits — `ai_engine/adaptive_pipeline.py`, `ai_engine/analysis/absence_detector.py`, `ai_engine/analysis/audit_trail.py`, `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/credibility_model.py`, `ai_engine/analysis/debate_prediction_verifier.py` … | 18 |
| `SUPABASE_URL` | yes | `gni_adaptive.yml`, `gni_graph.yml`, `gni_heartbeat.yml`, `gni_mad.yml`, `gni_market.yml`, `gni_pipeline.yml`, `gni_selfbias.yml` | `SUPABASE_URL` | 26 files, 57 hits — `ai_engine/adaptive_pipeline.py`, `ai_engine/analysis/absence_detector.py`, `ai_engine/analysis/audit_trail.py`, `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/credibility_model.py`, `ai_engine/analysis/debate_prediction_verifier.py` … | 2 |
| `TELEGRAM_ADMIN_CHAT_ID` | yes | `gni_mad.yml`, `gni_pipeline.yml` | `TELEGRAM_ADMIN_CHAT_ID` | 4 files, 7 hits — `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/keyword_sensor.py`, `ai_engine/groq_guardian.py`, `src/app/api/mission-control/route.ts` | 0 |
| `TELEGRAM_ADMIN_ID` | yes | `gni_adaptive.yml`, `gni_graph.yml`, `gni_heartbeat.yml`, `gni_mad.yml`, `gni_market.yml`, `gni_pipeline.yml`, `gni_selfbias.yml`, `gni_selfcheck.yml` | `ADMIN`, `TELEGRAM_ADMIN_ID` | 9 files, 17 hits — `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/gni_forensic_trace.py`, `ai_engine/analysis/grounding_watch.py`, `ai_engine/analysis/source_health_monitor.py`, `ai_engine/mad_runner.py`, `ai_engine/monitoring_pipeline.py` … | 0 |
| `TELEGRAM_BOT_TOKEN` | yes | `gni_adaptive.yml`, `gni_graph.yml`, `gni_heartbeat.yml`, `gni_mad.yml`, `gni_market.yml`, `gni_pipeline.yml`, `gni_selfbias.yml`, `gni_selfcheck.yml` | `TELEGRAM_BOT_TOKEN`, `TG` | 13 files, 26 hits — `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/gni_forensic_trace.py`, `ai_engine/analysis/grounding_watch.py`, `ai_engine/analysis/keyword_sensor.py`, `ai_engine/analysis/source_health_monitor.py`, `ai_engine/groq_guardian.py` … | 1 |
| `TELEGRAM_CHAT_ID` | yes | **none** | — | 1 files, 2 hits — `preflight.sh` | 7 |
| `TELEGRAM_QSChannel_ID` | yes | `gni_adaptive.yml`, `gni_heartbeat.yml`, `gni_mad.yml`, `gni_pipeline.yml` | `TELEGRAM_QSChannel_ID` | 10 files, 14 hits — `ai_engine/analysis/code_fix_suggester.py`, `ai_engine/analysis/grounding_watch.py`, `ai_engine/analysis/keyword_sensor.py`, `ai_engine/analysis/source_health_monitor.py`, `ai_engine/mad_runner.py`, `ai_engine/monitoring_pipeline.py` … | 10 |
| `TELEGRAM_WEBHOOK_SECRET` | yes | **none** | — | 1 files, 1 hits — `src/app/api/telegram-webhook/route.ts` | 1 |
| `TWELVE_DATA_API_KEY` | yes | `gni_pipeline.yml` | `TWELVE_DATA_API_KEY` | 2 files, 4 hits — `ai_engine/collectors/alpha_vantage.py`, `tools/mk_order_s95.py` | 2 |
| `VERCEL_BYPASS_SECRET` | yes | `gni_pipeline.yml` | `VERCEL_BYPASS_SECRET` | 1 files, 3 hits — `ai_engine/analysis/staging_checker.py` | 0 |

**Read the `none` rows with the S93 ruling in hand: a secret read by no workflow is not dangling. `GROQ_MODEL_FALLBACK` is read by code only; `GROQ_TEST_ONLY` is a deliberately local probe account; `TELEGRAM_CHAT_ID` is a pre-rename remnant that `preflight.sh` GUARDS AGAINST — its mention is a negative reference, so a nonzero consumer count there is expected. `TELEGRAM_WEBHOOK_SECRET` is read by Vercel, not Actions. Nothing in this table is a deletion instruction.**

### 7.3 Dependency lists — evidence for item 6.9

Dependency manifest at repo root: **NONE**. Each workflow therefore carries its own inline `pip install` list.

| workflow | packages installed inline |
|---|---|
| `gni_adaptive.yml` | `feedparser==6.0.12`, `geopy==2.4.1`, `supabase`, `python-dotenv`, `groq`, `requests` |
| `gni_ci_harness.yml` | `feedparser==6.0.12`, `geopy==2.4.1`, `supabase`, `python-dotenv`, `groq`, `requests` |
| `gni_graph.yml` | `supabase`, `python-dotenv`, `networkx` |
| `gni_heartbeat.yml` | `feedparser==6.0.12`, `geopy==2.4.1`, `supabase`, `python-dotenv`, `groq`, `requests` |
| `gni_mad.yml` (list 1 of 2) | `feedparser==6.0.12`, `geopy==2.4.1`, `supabase`, `python-dotenv`, `groq`, `requests` |
| `gni_mad.yml` (list 2 of 2) | `supabase`, `python-dotenv`, `requests` |
| `gni_market.yml` | `supabase`, `python-dotenv`, `requests` |
| `gni_pipeline.yml` (list 1 of 2) | `feedparser==6.0.12`, `geopy==2.4.1`, `supabase`, `python-dotenv`, `groq`, `requests`, `yake`, `rank_bm25`, `openpyxl` |
| `gni_pipeline.yml` (list 2 of 2) | `supabase`, `python-dotenv`, `requests`, `openpyxl` |
| `gni_selfbias.yml` | `supabase`, `python-dotenv` |
| `gni_selfcheck.yml` | — |

**6 distinct package sets across 10 install steps in 9 workflows. None is diffable against any other, because there is nothing to diff them against.**
## §8 CROSSCUTTING CONCEPTS

### 8.1 Two failure categories — the distinction that reframes the roadmap

| | RUNTIME failure | EPISTEMIC failure |
|---|---|---|
| What happens | something stops working | it works, but the record of it is wrong |
| Detectable by | health monitors, exit codes | **nothing currently in the system** |
| Examples | dead feed, retired model, API timeout | 3 harnesses dead 2 months; 8 rule IDs cited but unregistered; a deadline with no origin carried 11 generations; `limit(332)` decaying |
| Addressed by | the White Paper's self-healing design | the reference layer + mechanical document checks |

**The White Paper's self-healing design addresses the left column. Every failure
that has actually cost GNI sessions is in the right column.**

### 8.2 A system cannot repair what it cannot describe

Auto-repair requires a machine-readable model of what SHOULD be true. That model
is §5/§6/§7. The reference layer is therefore not a detour from Layer 2 — it is
Layer 2's prerequisite.

### 8.3 The path to needing fewer agents

GNI holds 134 engineering rules. All 134 are prose; all 134 require an LLM to read
and apply; **none is executable.** Yet many are mechanically checkable —
R-S91-5 is a `git grep`, LR-101 is an assert, R-S90-2 is a script.

> **Every judgment converted into a check is an agent no longer needed.**

This, not better agents, is the mechanism that reaches Layer 3.

### 8.4 Agents detect and execute; they do not decide

GNI already ran the multi-agent-debate experiment: MAD. The record shows
ARB-ARRIVAL starvation, depth=0, and 198 of 199 reports at exactly 10.0. Debate
between agents does not remove error — it can wrap one wrong answer in agreement.

**Unattended operation therefore needs pre-decided playbooks, not conversation:**

```
Layer 0  DETECTOR   assertions, exit codes, counts, diffs   — not an agent
Layer 1  TIER-1     pre-approved repairs from a registry    — not an agent
Layer 2  TIER-2     novel fault: classify, STOP LOUDLY, report — one agent, no write authority
Layer 3  GUARDIAN   boundaries, design, law                 — the operator
```

Audit integrity is never auto-repaired (White Paper, unchanged).

---

## §9 ARCHITECTURAL DECISIONS

Homed in `GNI_TARGET_AND_ORDER_S{N}.md` under CHANGED THIS REGENERATION
(CONTRACT v5, M3 ruling: no fifth document). The accepted cost is that decisions
are findable only by reading past order files. Industry practice would use one
file per decision under `docs/adr/`; GNI knowingly does not.

---

## §10 QUALITY REQUIREMENTS

### 10.0 The principle

Current target: **TRUTHFULNESS OF OUTPUT**. A cert must DISCRIMINATE (R-S90-1) — a check
that passes under both the correct and the incorrect implementation has certified nothing.

**GNI does not promise a cadence. GNI promises to tell the truth about its cadence.**

A promise about delivery would be a promise about GitHub's scheduler, which §4's ITIL SLM
row establishes is not ours. A promise nothing can falsify is not an SLO; a promise about
someone else's system is not an SLO either.

### 10.1 What GNI does NOT promise — stated first, on purpose

- **Schedule delivery.** See §4, ITIL SLM row, first entry.
- **The declared cron as an operating cadence.** `gni_heartbeat.yml` declares
  `0,30 * * * *`. That is a REQUEST, not a rate, and it has never been the delivered rate:
  even in the best regime measured, check p50 was 0.82 h against a declared 0.5 h.
- **`GNI-R-116`'s word "immediately".** The rule is not amended here. It is given the
  measured number it never had, in 10.2.

### 10.2 SLI — what is measured (indicators, never promises)

Every figure carries the window that produced it (DECISION S90-3) and the command that
produces it (R-S96-2). Only whole UTC days are counted, the convention
`tools/gni_runtime.py` already applies to §6.

**SLI-1 is the CHECK gap, not the run gap.** A heartbeat run inside a `GNI-R-122`
protection window returns at `ai_engine/monitoring_pipeline.py:317-320`, before it opens a
database connection, and performs no check while still reporting `success`. Counting such
a run as a heartbeat measures the workflow, not the watch.

| id | indicator | source |
|---|---|---|
| SLI-1 | **effective check gap** — p50 / p90 / max, per regime | run history minus protection windows |
| SLI-1b | run gap, same statistics | run history |
| SLI-2 | delivered / declared, per day, never a single window average | §6.1, §6.4 |
| SLI-3 | freshness — age of the newest escalation reading | = SLI-1 |

**MEASURED from `docs/gni_runtime_snapshot_S99.json`, window 2026-08-19 .. 2026-09-03,
using `gni_runtime.quantf` and `gni_runtime._dt` — the same quantile the rest of this
document is computed with, imported rather than reimplemented (R-S96-3).**

| regime | window | runs | checks | suppressed | check p50 | check p90 | check max |
|---|---|---|---|---|---|---|---|
| PRE | 2026-08-19 .. 08-26 | 241 | 203 | 38 | 0.82 h | 1.24 h | 3.95 h |
| **POST** | **2026-08-27 .. 09-03** | **41** | **29** | **12** | **5.33 h** | **11.05 h** | **30.61 h** |

Run gaps over the same windows, for comparison: PRE p50 0.79 / p90 1.15 / max 3.37; POST
p50 4.34 / p90 7.31 / max 11.30. **The distance between the run figures and the check
figures IS the SLO-1 violation, published rather than described.**

**A single check gap of 30.61 hours occurred in the POST window.** For that period GNI
performed no escalation check at all, and every workflow run inside it reported `success`.

**POST is the current regime.** PRE is retained because deleting it makes the change
invisible. **A single figure across both is REFUSED, not averaged** — the 37% in §6.1 and
the 226-minute median in §6.2 span this boundary and describe no day that happened.

```bash
python - <<'PY'
import json, sys
sys.path.insert(0, "tools")
from gni_runtime import quantf, _dt
def in_pw(t):                       # GNI-R-122; C7 reads these by AST, this probe hardcodes
    m = t.hour*60 + t.minute
    return (m >= 23*60 or m < 90) or (540 <= m < 645)
d = json.load(open('docs/gni_runtime_snapshot_S99.json', encoding='utf-8'))
ts = sorted(_dt(r['createdAt'])
            for r in d['workflows']['gni_heartbeat.yml']['runs'])
for lo, hi, tag in [("2026-08-19","2026-08-26","PRE "),
                    ("2026-08-27","2026-09-03","POST")]:
    run = [t for t in ts if lo <= t.strftime("%Y-%m-%d") <= hi]
    eff = [t for t in run if not in_pw(t)]
    g = sorted((eff[i+1]-eff[i]).total_seconds()/3600 for i in range(len(eff)-1))
    print("%s runs=%3d checks=%3d suppressed=%2d  p50=%5.2f p90=%6.2f max=%6.2f"
          % (tag, len(run), len(eff), len(run)-len(eff),
             quantf(g,0.5), quantf(g,0.9), g[-1]))
PY
```

### 10.3 SLO — three promises, all of them ours

| id | promise | target | error budget | checked by |
|---|---|---|---|---|
| **SLO-1 WATCH INTEGRITY** | every heartbeat run that STARTS completes its escalation check | 100% | none | **not wired — see below** |
| **SLO-2 FRESHNESS DISCLOSURE** | the published freshness bound is a TRUE p90 of the current regime's CHECK gaps | p90 | 10% of gaps | `C7` (a) |
| **SLO-3 REGIME INTEGRITY** | no published cadence figure spans a regime boundary | 100% | none | `C7` (b) |

**SLO-CFG** — the committed constants. `C7` reads these lines and holds no number of its
own, because `C5` forbids a check from carrying a hand-written expected integer (R-S81-5).
The document is the configuration.

- SLO-CFG BOUND_HOURS: `12`
- SLO-CFG EXCEEDANCE_MAX: `0.10`
- SLO-CFG WINDOW_FROM: `2026-08-27`
- SLO-CFG WINDOW_TO: `2026-09-03`
- SLO-CFG SPLIT_RATIO: `2.0`
- SLO-CFG WORKFLOW: `gni_heartbeat.yml`
- SLO-CFG SNAPSHOT: `docs/gni_runtime_snapshot_S99.json`

**SLO-1 — DECLARED, MEASURED, AND CURRENTLY VIOLATED.**
`monitoring_pipeline.py:317-320` returns `True` before `_get_supabase_client()`, so a run
inside a protection window performs no divergence check, no consensus check and no
escalation delta read, and reports `success`. `GNI-R-122` justifies protection windows by
**token collision**; `GNI-R-114` states the heartbeat uses **zero Groq**, and says so in
that same file at lines 4, 7, 313 and 466. A zero-Groq workflow cannot collide on tokens.
The rule was applied outside its subject.

The impact scales inversely with delivery, which is why it was harmless for five months:
in PRE it moved p90 from 1.15 h to 1.24 h; in POST it moves p90 from 7.31 h to 11.05 h and
max from 11.30 h to 30.61 h. **The defect did not change. The environment did, and nothing
went red.**

Second recorded instance of DEGRADE-SILENT (the S97 finding, `ctx-trim@0`): zero work
performed, `success` reported. **This closes order item 6.8 with its root cause.**

SLO-1 is deliberately NOT wired into `C7`. A check that is red from the day it ships trains
its readers to ignore it, and S98 spent a session making this detector green. The fix is
corrective work; 10.5 routes it without a ruling.

**SLO-2 — PUBLISHED FRESHNESS BOUND: 12 hours.**
Derived, not chosen: current-regime check p90 is 11.05 h, and the bound is that value
rounded up to the next whole hour. **GNI's view of the world is no more than 12 hours old,
nine times out of ten.** Every public page stating a monitoring cadence states THIS number
with THIS window, never `0,30 * * * *`. The bound falls when SLO-1 is fixed; publishing it
is what makes that visible.

At the published bound, 2 of the 28 POST check gaps exceed 12 h — **7.1%, inside the 10%
budget**. At 11 h, 4 of 28 exceed — **14.3%, outside it**. One hour of difference flips the
verdict, which is what makes the check discriminating rather than decorative.

### 10.4 Error budget — what it measures

The budget does not measure GitHub's failure. **It measures how stale GNI's own promise has
become.**

> Over the published window, at most **10%** of effective check gaps may exceed the
> published bound. That is not a chosen tolerance: it is what p90 means. If more than 10%
> exceed it, the published bound no longer describes the system — the number is wrong,
> whatever the cause.

Budget exhaustion is never evidence that GitHub broke a promise. GitHub made none. It is
evidence that GNI is publishing a figure that stopped being true. **This is the target,
stated as an operational limit.**

### 10.5 POLICY — the part that runs without James

> **When the SLO-2 budget is exhausted, the NEXT session's MISSION is automatically
> RELIABILITY DISCLOSURE** — recompute the bound from the current regime, republish it with
> its window, and record the regime change. It enters the top of the order without a ruling.
>
> Discharged when the published bound is a true p90 again. **NOT discharged by explaining
> why the gap grew.**
>
> **A cause that IS ours becomes a corrective item ranked above the next mission.** SLO-1 is
> the first such item, and is how the session after this one selects its mission without
> James declaring it.

If James is away for three weeks, GNI still notices that it has started lying about itself.

### 10.6 The check — `C7`, no new tool

`C7` lives in `tools/gni_rule_checks.py`. It reads the committed snapshot, never the
network, so it runs under `gni_ci_harness.yml`'s no-secrets boundary unchanged. It reads
every constant from SLO-CFG above, the protection windows from
`ai_engine/monitoring_pipeline.py` by AST, and its quantile from `gni_runtime.quantf`, so
no number in this section can drift from the number the check enforces (R-S96-3, the
precedent C6 set).

C7 fails when either:
- **(a)** BOUND_HOURS is not the smallest whole hour for which no more than
  EXCEEDANCE_MAX of the published window's effective check gaps exceed it. Too low
  fails on exceedance; too high fails because a smaller hour would also have passed.
  The bound is therefore DERIVED by the check, not merely compared against, so a
  padded bound cannot buy a green; or
- **(b)** the published window spans a regime boundary — split at the largest step, compare
  the two halves' p50, and refuse a single figure at SPLIT_RATIO or more.
  A split is COUNTED only when both halves hold at least `1 / EXCEEDANCE_MAX`
  gaps. That floor is derived, not chosen: below ten samples a p90 does not
  exist, and a one-gap half reports its own single gap as a median. Measured
  inside the published window, the uncounted splits peak at 2.46 and the
  counted ones at 1.47; across the regime boundary the counted peak is 6.47,
  and it falls on 2026-08-27 -- the instrument finds the date unaided.

C7 raises INSTRUMENT ERROR, not FAIL, when the snapshot does not span the published window.
*"This data cannot answer this question"* is a different answer from *"the promise was
kept"* (R-S81-1).

**CERT — every case below exists in stored history. No live run is waited for, and no
fixture is invented (R-S100-1).**

| case | perturbation | expected | measured |
|---|---|---|---|
| **MUST MISS (a)** | none — published `12` | **GREEN** | 2 of 28 = 7.1% |
| **MUST CATCH (a)** | BOUND_HOURS `12` -> `11` | **RED** | 4 of 28 = 14.3% |
| **MUST MISS (b)** | none — window 08-27 .. 09-03 | **GREEN** | one regime |
| **MUST CATCH (b)** | WINDOW_FROM -> `2026-08-19` | **RED** | check p50 ratio **6.47** |

Both perturbations are one character in this document. Neither touches the check.

### 10.7 What this section still does not answer

- **Why** a slot did not fire. §6.5 says this already; §4's ITIL SLM row is where the answer
  lives when it is not ours.
- **Whether reducing the declared frequency would raise delivered coverage.** Filed,
  [PROPOSED], not measured. The SLO is the instrument that would measure it, which is why
  the SLO came first.
- **Freshness of the DATA rather than the WATCH.** This bounds how old GNI's view is, not
  how old the articles inside it are. Deferred with this sentence rather than omitted.
- **Why POST suppression is 29% against PRE's 16%** when the protection windows cover 17.7%
  of the clock. Small sample (12 of 41) or clustering; not separated. Filed.
- **Whether `HARVEST_LIMIT = 300` still spans the window if delivery recovers.** At the
  current rate it spans about sixty days; at PRE's rate it spans about nine, and a 14-day
  window would silently truncate. C7's INSTRUMENT ERROR catches it; nothing prevents it.
- **Anything else about the process itself.** The rest of §11's diagnosis stands: every
  other instrument GNI owns still points outward.

---

## §11 RISKS & TECHNICAL DEBT — the S92 diagnosis

**D1 — UNSTABLE QUEUE.** Discovery policy mandates recording every weakness found;
one mission closes per session. Findings in: ~8 (S90), 3 (S91), 2+ (S92). Out: 1.
rho > 1. An unbounded queue is arithmetic, not indiscipline — and no document
measures this ratio.

**MEASURED AT S93, and the number needs a scope.** Read from each generation's own
`**CLOSED:**` and `**NEW ITEMS:**` lines: S90 = 6 in / 8 out (rho 0.75); S91 = 3 in /
3 out (rho 1.00); S93 = 9 in / 1 out (rho 9.00); S94 = 4 in / 1 out (rho 4.00). D1's estimate of "Out: 1" came from
"one mission closes per session" - true of missions, false of items, because certs and
disproven premises close items too.

**The scope: rho measures a session that WORKS THE QUEUE. It cannot measure a session
that SETS DIRECTION.** S92 produced this document, five discipline rulings, five
DECISIONS and the S93-S96 roadmap, and scores ~1 - which reads as "S92 did nothing".
The metric is sound inside its scope and misleading outside it. Sessions must therefore
be typed before rho is compared across them.

**Second hazard, from the same measurement:** S92 correctly homed D1-D7 here rather than
in the order file, and the queue-side numerator lost them. Routing a finding out of the
order removes it from the count. Fixed at S93 by requiring the order's `**NEW ITEMS:**`
line to name findings routed elsewhere, with a pointer.

**D2 — KNOWLEDGE STORED AS PROSE, NOT TABLES.** Prose cannot be queried, diffed,
or made to fail loudly when stale. Evidence: `GNI-R-076` cited with the wrong
meaning for ~35 sessions; "62 unique ids" stated wrongly by two consecutive
sessions (actual 47); the published band table wrong in 2 of 5 rows. The GRAVEYARD
is the sole structure copied BY BYTES, and the sole structure not observed to rot.

**D3 — SIX EMPTY DRAWERS.** Of arc42's 12 sections, GNI populated 9 and 11
(decisions, debt) and left 1, 3, 4, 5, 6, 7, 8, 12 empty. The empty ones are
exactly the "what IS" sections. Consequence: tech debt is the only drawer that can
grow, so everything found becomes debt.

**D4 — ARCS END BY RENAMING, NOT BY COMPLETION.** SUBPAGE-IC (Mar) → named at S69
→ deliverable `SUBPAGE_CERTIFICATION.md` created S70 → re-minted as ROOT 9 at S85.
Five months, no completion declaration, and no live document points at the
deliverable. "When will it end" is therefore unanswerable by construction.

**D5 — NO TEST RUNS IN CI.** 10 local harnesses, 42 `__main__` selftests, zero
executed by any workflow. Three harnesses died on 2026-06-27 (`c3ce662`) and
failed loudly, unread, for two months. CI would have caught this on the first push.
**Highest-leverage item in this document; smallest cost.**

**D6 — THE AGENT IS THE INPUT PUMP.** The numerator of rho is Claude. This is a
structural mismatch between measurement rate and adjudication rate, not a fault of
either party. Full analysis: the S92 AI-agent SWOT (W1–W7), summarised: confident
recall is indistinguishable from knowledge from the inside; instrument errors
dominate (3 of 3 this session); the agent's leans run toward doing work, never
toward deleting it.

**D7 — FIXED CONSTANTS IN A MOVING WORLD.** One shape, two instances the same day:
a calendar deadline ("Aug 9") carried 11 generations with no recoverable origin,
and `limit(332)`, an absolute offset into a growing table that decayed within
hours. Both fixed by selecting on a RELATION, not a position.

---

## §12 GLOSSARY

**BEV** — bird's-eye view; read before editing. · **Canary** — an unprotected
component whose failure is the signal. · **GRAVEYARD** — falsified designs, copied
forward by bytes so they are never re-minted. · **LINEAGE** — the required
evidence line on every proposal. · **rho** — findings in ÷ items closed, per
session. · **Epistemic failure** — see §8.1. · **Toil** — manual work that scales
with the system and produces no lasting value (SRE).

---

## ROADMAP TO LAYER 2

| Session | Work | Which layer it builds |
|---|---|---|
| S93 | ✅ **DONE** — `gni_ci_harness.yml`, commit `944c4f0`, cert run `33529254247` (RED, discriminating) | Layer 0 detector — first brick |
| S94 | ✅ **DONE** — `tools/gni_state.py`, commit `44a3cba`, §7 only; discriminating cert passed | the world model Layer 2 needs |
| S95 | ✅ **DONE** — `b639b54` + `6989fad`; the mission said "all 134 rules", the measurement found **159** (57 CHECKABLE / 102 not, R-S96-2) — the 134 was banked with no method (R-S95-4) | reduces agent dependence |
| S96 | ✅ **DONE** — `tools/gni_macro_map.py` + `docs/GNI_MACRO_MAP_S95.md`, commit `09588b2`; discriminating cert passed, three negative controls exit 2. Its output was named for the REGISTER it read, not the session; S98 fixed the generator (5.27) and the live map is `docs/GNI_MACRO_MAP_S98.md` (DECISION S98-3) | measures the gap instead of estimating it |

One mission per session. Each is a single deliverable.

### ROADMAP ACHIEVED - 2026-09-03 (S96 close, DECISION S96-1)

**All four rows are DONE.** Evidence, in order: `944c4f0` CI detector (S93) - `44a3cba` §7 state
generator (S94) - `b639b54` rule to check (S95; 57 CHECKABLE of 159, five shipped) - `09588b2`
macro map (S96). This arc is ARCHIVED. New work in the same area goes in the order under its
ROOT; it does not reopen this table.

**The TARGET is NOT achieved.** TRUTHFULNESS OF OUTPUT stands, with 9.13, 9.15, 9.16, 9.17 and
DEGRADE-SILENT open beneath it. What ended is the ROADMAP, not the target (CONTRACT v10).

## ROADMAP 2 - SMART OFFICE (S98-S101), declared by James at the S96 close

| Session | Mission | What it buys |
|---|---|---|
| S98 | ✅ **DONE** — 5.14+5.17 `98a9bc3`, 9.15 `173bce9`, M4 `a5f2813`, C6 `1d5bcab`; cert pair `447ce55` RED / `328be08` GREEN, read at job level | the detector goes GREEN, so RED carries information again |
| S99 | ✅ **DONE** -- `2bfef91`, CI run `33851242047` green at JOB level (roadmap 2 row 2 shipped at S99) | delivered cadence measured against declared cadence, regenerating byte-identically from a tracked snapshot |
| S100 | ARCHITECTURE §5 Building Block View, GENERATED (AST) | what imports what, without a grep |
| S101 | SLO + error budget + policy | a published number that says whether the system met its own promise |

**S97 DOES NOT EXIST as a build session.** It was an advisory session held in a separate chat:
no commits, no numbered artifacts, no `*_S97.md` files. Its entire output is this roadmap and
the DEGRADE-SILENT finding, both merged into order generation 16. Nothing from S97 is missing.

**This table is deliberately NOT under the `## ROADMAP TO LAYER` heading.** `tools/gni_macro_map.py`
parses the FIRST such heading only, so roadmap 2 is invisible to the macro map's Y axis - which is
correct today, because no row of it has been built and a plan is not a measurement. When S98 ships
C6 it should decide, in writing, whether planned rows enter the map as rows-without-a-layer.

**DECISION S98-5 (S98 close, the decision this paragraph demanded in writing).** Planned rows
do NOT enter the macro map. The Y axis binds a session to a LAYER it built, and a row that has
not been built has no layer to bind - entering it would put an intention on an axis whose whole
purpose is to separate what is claimed from what is measured, which is the target this system
declared. Roadmap 2's rows enter the map one at a time, as each ships, through the ROADMAP TO
LAYER table above. S98's own row is the first to do so.


### COMPLETION TEST for ROADMAP 2 (CONTRACT v10 - a roadmap declared without one is not declared)

COMPLETE when all four hold, each answerable from bytes by the command beside it:

| # | The test | The command |
|---|---|---|
| 1 | `gni_ci_harness.yml` reaches conclusion `success` on a push to `main` with no code change - BOTH jobs green | `gh run view <id> --json jobs --jq '.jobs[] | [.name,.conclusion] | @tsv'` |
| 2 | §5, §6 and §7 are each GENERATED by a named tool in `tools/`, and each regenerates byte-identically on an unchanged tree | run each tool twice, `md5sum` both outputs |
| 3 | §10 carries a written SLO with an error budget, and one measured value per SLO published with the command that produced it | `grep -n 'SLO' docs/GNI_ARCHITECTURE_S*.md` |
| 4 | `tools/gni_rule_checks.py` exits 0 on a clean tree and RED when §5, §6, §7 or the macro map is stale against its source | flip one source figure, expect exit 1 |

Until all four hold, the answer to "is Smart Office finished?" is **NO**, and the failing row is
named. D4 (§11) ran five months with deliverables and no way to ask this question. It can now be
asked in four commands.

### STATUS OF THE COMPLETION TEST AT THE S98 CLOSE (row 1 of roadmap 2 shipped)

| # | Holds? | Evidence |
|---|---|---|
| 1 | **YES** | run `33810361813` at `328be08` - a push whose tree is byte-identical to `98a9bc3` (`git diff HEAD~2 --stat` empty) reached `conclusion: success` with `rule_checks success` and `harnesses success` read at JOB level. Re-confirmed at `1d5bcab`, run `33812669304`. |
| 2 | **NO** | §6 is generated and byte-identical (`tools/gni_runtime.py`, S99, md5 `fb6e3f1e0e96e6a696af988b08bb6143` on two renders). §5 is S100. **AND §7 FAILS THIS ROW ON ITS OWN ACCOUNT** -- `gni_state.py` renders `datetime.now()` into its own stamp, so two runs two seconds apart differ (`06:14:00Z` vs `06:14:04Z`). Item **5.33**. The S98 status recorded this row as failing only because §5 and §6 were missing; that was incomplete. |
| 3 | **NO** | no SLO is written. S101. |
| 4 | **PARTIAL** | `rule_checks` exits 0 clean and RED on a stale macro map (C6, item 5.26, `1d5bcab`) and on a stale §7 (C2, fixture family `5-stale-generator`). §5 and §6 cannot be stale because they do not exist. |

The failing rows are named, which is the point of the test. Roadmap 2 is **1 of 4**.



